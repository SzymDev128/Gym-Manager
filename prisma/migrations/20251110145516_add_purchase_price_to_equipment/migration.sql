BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Equipment] ADD [purchasePrice] FLOAT(53) NOT NULL CONSTRAINT [Equipment_purchasePrice_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
